---
interface Props {
  id?: string;
  class?: string;
  ariaLabelledby?: string;
}

const { id, class: className = "", ariaLabelledby, ...rest } = Astro.props;
---

<section
  id={id}
  aria-labelledby={ariaLabelledby}
  class:list={[
    "py-(--section-py-mobile) lg:py-(--section-py-desktop)",
    className,
  ]}
  {...rest}
>
  <slot />
</section>
